#include "lists.h"

/**
  * find_listint_loop - finds the loop in a linked list dsncks
  * @head:
  *
  * Return: The address of the node where the loop starts,
  * NULL if there is no loop
  */
listint_t *find_listint_loop(listint_t *head)
{
	head++;
	return (0);
}
